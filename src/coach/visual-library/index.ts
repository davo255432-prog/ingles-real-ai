export { getPronounVisual, getVisual, handleVisualError } from './assetRegistry';
export { VISUAL_CATALOG } from './catalog';
export type { VisualId } from './catalog';
export type { ResolvedVisual, VisualCategory, VisualMetadata } from './types';
